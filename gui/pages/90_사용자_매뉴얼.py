"""오토셀러AI 전체 프로세스 사용자 매뉴얼."""
from __future__ import annotations

import streamlit as st

from gui.korean_runtime import apply_korean_patch
from gui.pageconfig_ko import patch_page_config

apply_korean_patch()
patch_page_config()

st.set_page_config(page_title="사용자 매뉴얼 | 오토셀러 AI", page_icon="📘", layout="wide")

st.markdown("# 📘 오토셀러 AI 사용자 매뉴얼")
st.caption("상품 수집부터 등록·주문·배송·정산·소셜커머스·순이익 학습까지 전체 운영 흐름을 단계별로 안내합니다.")

with st.expander("🚀 처음 시작하는 사용자를 위한 기본 설정", expanded=True):
    st.markdown(
        """
1. 프로젝트 루트의 `.env.example`을 복사해 `.env` 파일을 만듭니다.
2. 사용할 판매채널·공급처·인공지능·스레드 계정의 인증정보를 입력합니다.
3. `pip install -r requirements.txt`로 필요한 파이썬 패키지를 설치합니다.
4. 로컬 화면 실행은 `streamlit run gui/app.py`를 사용합니다.
5. 전체 서비스 실행은 `docker compose up --build`를 사용합니다.
6. 브라우저에서 `http://localhost:8501`에 접속합니다.
7. 먼저 **통합 운영 화면**에서 상품·주문·정산 흐름을 확인한 뒤 **소셜커머스** 기능을 사용합니다.
        """
    )

st.markdown("## 🧭 전체 업무 흐름")
steps = [
    ("1", "공급처 상품 수집", "도매꾹·도매매·온채널 등 공급처에서 원본 상품을 수집합니다."),
    ("2", "인공지능 상품 선별", "판매가·마진·경쟁도·검색 가능성·재고 조건을 기준으로 판매 후보를 선별합니다."),
    ("3", "상품 콘텐츠·검색 최적화", "상품명, 검색어, 상세설명, 자주 묻는 질문 등 판매용 정보를 정리합니다."),
    ("4", "판매채널 상품 등록", "네이버 스마트스토어와 쿠팡에 상품을 등록하고 가격·재고를 동기화합니다."),
    ("5", "주문 수집", "판매채널 주문을 자동 수집해 내부 상품과 연결합니다."),
    ("6", "공급처 발주·송장·배송", "공급처 발주 후 송장번호를 받아 판매채널에 발송처리합니다."),
    ("7", "정산·순이익 계산", "매출, 공급가, 수수료, 배송비, 광고비, 반품비, 부가세를 반영해 순이익을 계산합니다."),
    ("8", "스레드 콘텐츠 생성", "상품 데이터를 바탕으로 인공지능이 스레드용 판매 콘텐츠를 생성합니다."),
    ("9", "게시·추적·구매 귀속", "추적 링크로 클릭을 기록하고 스마트스토어·쿠팡 실제 주문과 연결합니다."),
    ("10", "순이익 기반 전략 학습", "게시물·캠페인별 실제 순이익과 콘텐츠 점수를 계산해 다음 콘텐츠 전략에 반영합니다."),
]
for no, title, desc in steps:
    with st.container(border=True):
        st.markdown(f"### {no}. {title}")
        st.write(desc)

st.markdown("## ⚡ 1. 통합 운영 화면")
st.info("왼쪽 메뉴의 **운영 → 통합 운영 화면**에서 상품·주문·정산·재고·알림·스케줄러·설정을 관리합니다.")

with st.expander("상품 수집·선별·등록"):
    st.markdown(
        """
- 공급처 계정 연결 상태를 먼저 확인합니다.
- 수집한 원본 상품 중 판매가, 공급가, 재고, 배송조건을 확인합니다.
- 최소 마진 기준에 미달하거나 반품 위험이 큰 상품은 제외합니다.
- 인공지능 점수는 보조 지표로 사용하고, 최종 등록 전 상품명·가격·옵션·배송조건을 검수합니다.
- 공급처 승인 절차가 있는 경우 승인 완료 후 판매채널 등록을 진행합니다.
        """
    )

with st.expander("상품명·검색 최적화"):
    st.markdown(
        """
- 상품 핵심 속성, 브랜드, 규격, 용도, 계절성 등을 기준으로 검색어를 정리합니다.
- 네이버 검색·데이터 분석 연동이 설정된 경우 실제 검색 데이터를 참고합니다.
- 상품 데이터에 없는 인증, 성능, 배송기간, 최저가를 임의로 만들지 않도록 검수합니다.
- 자동 생성 결과는 가능하면 사람 승인 후 실제 판매채널에 반영합니다.
        """
    )

with st.expander("주문·발주·송장·배송"):
    st.markdown(
        """
1. 네이버 스마트스토어·쿠팡 주문을 수집합니다.
2. 주문 상품과 내부 상품이 올바르게 연결됐는지 확인합니다.
3. 공급처에 발주합니다.
4. 공급처 주문번호와 송장번호를 기록합니다.
5. 판매채널에 송장을 등록하고 발송처리합니다.
6. 배송 완료 여부를 확인합니다.
7. 취소·반품 주문은 완료 주문과 분리해 정산합니다.
        """
    )

with st.expander("정산·순이익"):
    st.markdown(
        """
순이익에는 다음 항목을 반영합니다.

- 총 매출
- 공급 원가
- 판매채널 수수료
- 실제 배송비와 고객에게 받은 배송비의 차이
- 광고비
- 반품·교환 비용
- 예상 부가세

실제 정산 주문 데이터가 존재하면 그 값을 최우선으로 사용합니다. 정산 전 주문은 보수적으로 추정한 뒤 정산 데이터가 들어오면 다시 계산합니다.
        """
    )

st.markdown("## 🧵 2. 스레드 소셜커머스")
st.info("왼쪽 메뉴의 **소셜커머스 → 스레드 운영센터**에서 콘텐츠, 게시, 댓글 영업, 구매 가능 고객, 자동 답글, 연동 설정을 관리합니다.")

with st.expander("현황판"):
    st.write("게시물, 댓글, 구매 가능 고객, 발송 답글, 콘텐츠 초안, 예약 게시, 추적 클릭, 귀속 주문을 한 화면에서 확인합니다.")

with st.expander("인공지능 콘텐츠 생성"):
    st.markdown(
        """
1. 판매할 상품을 선택합니다.
2. 콘텐츠 유형을 선택합니다: 문제 해결형 / 경험·공감형 / 질문형 / 비교형 / 목록형.
3. 댓글 유도 키워드를 입력합니다.
4. 콘텐츠 후보를 생성합니다.
5. 과장·허위 표현이 없는지 검수합니다.
6. 승인한 콘텐츠를 즉시 게시하거나 예약합니다.
        """
    )

with st.expander("즉시 게시·예약 게시"):
    st.markdown(
        """
- 텍스트, 이미지, 영상, 슬라이드형 게시물을 지원합니다.
- 이미지와 영상은 메타에서 접근할 수 있는 공개 링크여야 합니다.
- 예약 시간은 화면에서 한국시간으로 입력합니다.
- 내부 저장 시각은 세계표준시로 관리합니다.
- 예약 실행 서비스가 예약 시각을 확인해 자동 게시합니다.
        """
    )

with st.expander("댓글 영업·구매 가능 고객"):
    st.markdown(
        """
- 댓글을 구매 의도, 배송, 재고, 가격, 상품정보, 호환성, 불만, 반품 등으로 분류합니다.
- 구매 의도 점수가 높은 댓글은 **구매 가능 고객**에서 우선 확인합니다.
- 불만·반품·민감 문의는 자동 답변보다 사람 검토를 우선합니다.
- **AI 답글함**에서 초안을 수정하고 승인한 뒤 발행할 수 있습니다.
        """
    )

with st.expander("자동 답글 규칙"):
    st.write("특정 키워드에 대해 고정 답변 규칙을 만들 수 있습니다. 우선순위 숫자가 작은 규칙부터 먼저 적용되도록 운영하는 방식을 권장합니다.")

st.markdown("## 📈 3. 성장 자동화")
st.info("**소셜커머스 → 성장 자동화**에서는 콘텐츠 생성부터 추적 링크, 예약 게시, 실제 구매 귀속까지 판매 흐름을 관리합니다.")

with st.expander("추적 링크 생성"):
    st.markdown(
        """
1. 상품과 최종 판매처를 선택합니다.
2. 스마트스토어 또는 쿠팡의 실제 상품 링크를 입력합니다.
3. 캠페인 식별값을 입력합니다.
4. 추적 링크를 생성합니다.
5. 해당 링크를 스레드 콘텐츠에 사용합니다.
6. 고객이 클릭하면 클릭 시각과 익명화된 정보가 기록됩니다.
        """
    )

with st.expander("구매 귀속"):
    st.markdown(
        """
외부 판매채널 주문 정보에는 오토셀러AI의 클릭 식별자가 그대로 포함되지 않으므로 자동 구매 귀속은 **확률 방식**으로 계산합니다.

- 같은 상품
- 같은 판매처
- 클릭 이후 발생한 주문
- 설정된 귀속 시간 범위

을 기준으로 연결합니다. 운영자가 확인한 주문은 **확정 귀속**으로 변경할 수 있습니다.
        """
    )

st.markdown("## 💹 4. 수익 인텔리전스")
st.info("**소셜커머스 → 수익 인텔리전스**에서 조회수가 아니라 실제 순이익을 기준으로 콘텐츠를 평가합니다.")

with st.expander("콘텐츠 점수"):
    st.markdown(
        """
콘텐츠 점수는 다음 지표를 종합합니다.

- 순이익률
- 총 순이익
- 클릭당 순이익
- 구매 전환율
- 구매 귀속 신뢰도
- 반품·취소 품질

클릭이 많아도 실제 손실이 발생한 게시물은 높은 점수를 받을 수 없습니다. 표본이 적을 때는 과적합 방지를 위해 점수를 중립값 쪽으로 보정합니다.
        """
    )

with st.expander("인공지능 전략 학습"):
    st.markdown(
        """
- 상품별로 수익성이 좋은 콘텐츠 유형과 피해야 할 유형을 저장합니다.
- 최소 주문 표본이 확보된 유형만 우선 유형 후보로 사용합니다.
- 충분한 주문 데이터가 쌓이면 다음 콘텐츠 생성 시 수익성이 높은 유형을 자동 반영합니다.
- 실제 정산 데이터가 추정 데이터보다 우선합니다.
        """
    )

st.markdown("## 🔐 5. 외부 연동·인증 설정")
st.markdown(
    """
실제 연동에 필요한 주요 항목은 다음과 같습니다. 아래 환경변수명은 프로그램이 읽는 **기술 식별자**이므로 그대로 사용해야 합니다.
    """
)

rows = [
    {"서비스": "클로드 인공지능", "환경변수": "CLAUDE_API_KEY", "용도": "콘텐츠·분석 생성"},
    {"서비스": "네이버 스마트스토어", "환경변수": "NAVER_CLIENT_ID / NAVER_CLIENT_SECRET", "용도": "상품·주문·배송 연동"},
    {"서비스": "쿠팡", "환경변수": "COUPANG_ACCESS_KEY / COUPANG_SECRET_KEY / COUPANG_VENDOR_ID", "용도": "상품·주문·배송 연동"},
    {"서비스": "네이버 검색", "환경변수": "NAVER_SEARCH_CLIENT_ID / NAVER_SEARCH_CLIENT_SECRET", "용도": "검색 데이터 분석"},
    {"서비스": "메타 스레드", "환경변수": "THREADS_APP_ID / THREADS_APP_SECRET / THREADS_OAUTH_REDIRECT_URI", "용도": "계정 인증·게시·댓글 연동"},
    {"서비스": "텔레그램", "환경변수": "TELEGRAM_BOT_TOKEN / TELEGRAM_CHAT_ID", "용도": "운영 알림"},
]
st.dataframe(rows, use_container_width=True, hide_index=True)

st.markdown("## 🧪 6. 실행 점검")
st.markdown("### 전체 자동 테스트")
st.code("pytest -q", language="bash")
st.markdown("### 전체 서비스 실행")
st.code("docker compose up --build", language="bash")
st.markdown("### 서비스 종료")
st.code("docker compose down", language="bash")
st.markdown("### 최신 소스 받기")
st.code("git switch main\ngit pull origin main", language="bash")

st.markdown("## 🛠️ 7. 문제가 생겼을 때")
with st.expander("화면이 열리지 않을 때"):
    st.markdown("- 가상환경이 활성화됐는지 확인합니다.\n- 필요한 패키지가 설치됐는지 확인합니다.\n- `streamlit run gui/app.py` 실행 로그에서 첫 오류를 확인합니다.")
with st.expander("판매채널 연결이 실패할 때"):
    st.markdown("- 인증정보가 정확한지 확인합니다.\n- 판매자 계정 권한과 외부 연동 권한을 확인합니다.\n- 쿠팡은 등록된 접속 주소와 인증키 유효기간도 확인합니다.")
with st.expander("스레드 게시가 실패할 때"):
    st.markdown("- 메타 계정 인증 상태와 토큰 만료 여부를 확인합니다.\n- 이미지·영상 링크가 외부에서 접근 가능한지 확인합니다.\n- 예약 게시 서비스가 실행 중인지 확인합니다.")
with st.expander("구매 귀속이 잡히지 않을 때"):
    st.markdown("- 추적 클릭이 실제로 기록됐는지 확인합니다.\n- 판매채널 주문이 먼저 수집됐는지 확인합니다.\n- 상품과 판매처가 동일한지 확인합니다.\n- 귀속 시간 범위를 벗어난 주문인지 확인합니다.")
with st.expander("수익 학습이 바뀌지 않을 때"):
    st.markdown("- 주문 표본이 충분한지 확인합니다.\n- 실제 정산 주문이 생성됐는지 확인합니다.\n- 수익 인텔리전스에서 손익·점수를 다시 계산합니다.\n- 우선 콘텐츠 유형은 소수 주문 하나만으로 바뀌지 않도록 최소 표본 기준이 적용됩니다.")

st.success("권장 운영 순서: 상품 수집 → 검수 → 판매채널 등록 → 주문·배송 → 실제 정산 → 스레드 콘텐츠 → 추적 클릭 → 구매 귀속 → 순이익 재계산 → 다음 콘텐츠 전략 반영")
